#include "iostream"
using namespace std;
int main()
{
    int n, q;
    cin >> n >> q;
    string a[200050];
    for (int i = 1; i <= n;i++)
        cin >> a[i];
    while(q--)
    {
        int l, r;
        cin >> l >> r;
        int cnt = 0;
        int flag_9=0;
        int flag_1=0;
        int flag_0=0;
        int flag_2=0;

        while(l<=r)
        {
            if(a[l])
        }
    }
}